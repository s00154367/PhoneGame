@model PhoneGame.Models.Question

@{
    List<string> answers = new List<string>();
    answers.Add(Model.CAns);
    answers.Add(Model.Ans1);
    answers.Add(Model.Ans2);
    answers.Add(Model.Ans3);

    Random rd = new Random();
    int ind = 0;
}

<div>
    <div>
        <h2>
            @Html.DisplayFor(model => model.Q)
        </h2>
        <h3>
            @{ind = rd.Next(answers.Count()); }
            @answers[ind]
            @answers.Remove(answers[ind])
        </h3>
        <h3>
            @{ind = rd.Next(answers.Count()); }
            @answers[ind]
            @answers.Remove(answers[ind])
        </h3>
        <h3>
            @{ind = rd.Next(answers.Count()); }
            @answers[ind]
            @answers.Remove(answers[ind])
        </h3>
        <h3>
            @{ind = rd.Next(answers.Count()); }
            @answers[ind]
            @answers.Remove(answers[ind])
        </h3>
    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
